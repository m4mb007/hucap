<%= form_with(model: @course_category, url:course_categories_path, method: :post) do |f| %>
    <%= f.label :name %>
    <%= f.text_field :name%>

    <%= f.label :venue %>
    <%= f.text_field :venue %>
    <%= f.submit 'Create Training'%>
<% end %>

 <%= link_to 'Back', participants_path %>